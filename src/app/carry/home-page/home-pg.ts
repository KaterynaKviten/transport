import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-home-pg',
  templateUrl: './home-pg.html',
  imports: [RouterLink],
})
export class HomePage {}
